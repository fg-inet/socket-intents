
INCLUDE_DIRECTORIES(${GLIB2_INCLUDE_DIR})

ADD_LIBRARY(intents SHARED libintents.c)
TARGET_LINK_LIBRARIES(intents muacc ${GLIB2_LIBRARIES})

INSTALL(TARGETS intents
    LIBRARY DESTINATION lib
    RUNTIME DESTINATION bin
)
